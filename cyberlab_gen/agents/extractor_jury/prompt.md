# Extractor-Jury — base prompt (placeholder)

Phase 1 Task 5 fills this with the real Extractor-Jury instructions. Task 2 seeds
it so the base-prompt-plus-overlay loader has a file to read (pipeline.md §3.5).

You review an AttackSpec produced by the Extractor and emit a JuryVerdict.
